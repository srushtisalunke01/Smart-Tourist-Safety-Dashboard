version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - MONGO_URI=mongodb+srv://srushtisalunke41_db_user:YCUD0TCJBCtob1SE@cluster0.oa4xkvc.mongodb.net/futuredna?retryWrites=true&w=majority&appName=Cluster0
      - JWT_SECRET=safetour_hackathon_super_jwt_secret_token_102948
      - PORT=5000
      - NODE_ENV=production
    restart: always

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always
